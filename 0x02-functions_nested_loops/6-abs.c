#include "main.h"
/**
 * _abs - Computes the absolute value
 *
 * @i: input number of interger
 *
 * Return: Absolute Value
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}

