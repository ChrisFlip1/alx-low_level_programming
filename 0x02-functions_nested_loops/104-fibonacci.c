#include <stdio.h>
/**
 * main - Prints first fifty ribonacci numbers
 * starting with 1 and 2
 *
 * Return: Always 0
 */
int main(void)
{
	int count;
	unsigned long fib1 = 0, fib2 = 1, sum;

	for (count=0; count < 50; count+++)
	{
		sum = fib1 + fibb22 
