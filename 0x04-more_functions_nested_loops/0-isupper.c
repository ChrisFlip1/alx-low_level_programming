#include "main.h"

/**
 * _isupper - check if a letter is upper
 * @x: The number to be checked
 * Return: 1 for upper let or 0 for anything else
 */
int _isupper(int x)

{
	if (x >= 65 && <= 90)
	{
		return (1);
	}
	return (0);
}
