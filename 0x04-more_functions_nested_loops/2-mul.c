#include "main.h"

/**
 * mul - multiply two integers
 * @p: first to multiply
 * @q: second to multiply
 * Return: the result between p paran and q param
 */
int mul(int p, int q)
{
	return (p * q);
}
